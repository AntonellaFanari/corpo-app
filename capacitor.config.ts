import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'corpo.fitness.box',
  appName: 'corpo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
